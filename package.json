{
  "name": "tas-fighter",
  "version": "1.0.0",
  "description": "TAS Fighter",
  "main": "dist/main.js",
  "scripts": {
    "start": "yarn build && electron . --no-sandbox",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write ."
  },
  "author": "Joseph Valdez",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.15.2",
    "concurrently": "^9.1.2",
    "electron": "^28.1.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "wait-on": "^8.0.3"
  }
}
